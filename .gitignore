# OS files
Thumbs.db
Desktop.ini
.DS_Store

# Secrets / credentials (CRITICAL — never commit)
.env
.env.*
!.env.example
*.pem
*.key
*.p12
secrets/
credentials/
service-account*.json
.supabase/
firebase-config.json

# API keys patterns (defense-in-depth, use git-secrets hook recommended)
# Recommendation: install https://github.com/awslabs/git-secrets
# Then: git secrets --register-aws · git secrets --add 'sk-[a-zA-Z0-9]+'

# Editor
.vscode/
.idea/
*.swp
*.swo

# Gemini AI
.gemini/

# Node (if ever added)
node_modules/
package-lock.json

# PDF/docs not needed in repo
PDF,vocation/

# Local planning / design docs (keep local only)
implementation_plan.md
hsk_game_spec.md
DEPLOY_GUIDE.md
*.local.md

# Reports & migrations — keep local, don't push to GitHub
Bao_cao_*.md
Bug_Report_*.md
BUG_REPORT*.md
Optimization_Plan_*.md
extension_plan.md
sync_fix_*.md
*.sql

# docs/ — keep ENTIRE folder local only (2026-05-24)
# Lý do: docs/ không ảnh hưởng web app (chỉ planning + AI context)
# Repo PUBLIC — tránh leak business strategy + pricing + revenue projection
docs/

# Local-only assets — không cần cho production Cloudflare deploy
# (2026-05-17) data prep tools + raw curriculum private
scripts/
content/

# Backend Edge Functions — deployed via Supabase Dashboard manual,
# không liên quan Cloudflare. (2026-05-17 user choice: keep local-only)
# CẢNH BÁO: file chỉ tồn tại local — mất laptop = mất source code.
# Phải copy text trong Supabase Dashboard về để recreate nếu cần.
supabase/

# Mai audio — served from Cloudflare R2 (cdn.hanzigenz.com), NOT git.
# (2026-06-01) Tránh phình repo (676 file HSK1+2, sẽ lên 100MB+ với HSK3-9).
# Gen bằng scripts/mai-tts-gen.py → upload R2 bằng rclone (xem ACTION_ITEMS.md).
assets/mai/audio/
# Audio TTS pre-gen từ điển/pinyin (gen-tts-audio.py → R2 tts/word/). Phục vụ từ R2.
assets/tts/audio/

# Temp word lists
_hsk*_existing.txt

# Backup / temp files
*.backup
*.bak
*.orig
*~

# Database dumps
*.dump
*.sqlite
*.db

# Defensive: secret patterns (chưa có nhưng phòng tương lai)
.env
.env.*
*.pem
*.p12
*.pfx
*.key
credentials.json
service_account*.json
firebase-adminsdk-*.json
config.local.*
*.local.json

# Claude Code bootstrap (working style — auto-loaded by Claude Code)
CLAUDE.md
.claude/

# Playwright MCP cache (browser session traces)
.playwright-mcp/

# Working tree debug artifacts (network logs, screenshots from local QA)
*-network.txt
*-warnings.txt
*-errors-*.txt

# Local QA screenshots (verify-*.png, admin-*.png, radicals-*.png, phase screenshots, learn-hub screenshots)
verify-*.png
admin-*.png
radicals-*.png
phase-p9-*.png
learn-hub-*.png
*-test.png
*-result.png
hskk-*.png

# Demo / mockup folders (local prototyping only)
demo/
hanzi-genz-mockup/

# Competitor notes (business intel — never public)
topikmaster-home.md
corodomo-*.md

# Marketing / launch assets — keep local only (repo PUBLIC, giữ repo gọn)
Huong-dan-*.docx
beta-test-*.md
email-gui-tester.md
fb-cover.html
fb-post.html
tiktok-post.html
tiktok-post.png
tao-form-khao-sat.gs
gen-docx.js

# Page text snapshots + marketing screenshots (local QA / marketing only)
hg-*.md
hg-*.png
