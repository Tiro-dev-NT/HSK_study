# OS files
Thumbs.db
Desktop.ini
.DS_Store

# Editor
.vscode/
.idea/
*.swp
*.swo

# Gemini AI
.gemini/

# Node (if ever added)
node_modules/
package-lock.json

# PDF/docs not needed in repo
PDF,vocation/

# Local planning / design docs (keep local only)
implementation_plan.md
hsk_game_spec.md
DEPLOY_GUIDE.md
*.local.md

# Reports & migrations — keep local, don't push to GitHub
Bao_cao_*.md
Bug_Report_*.md
BUG_REPORT*.md
Optimization_Plan_*.md
extension_plan.md
sync_fix_*.md
*.sql

# docs/ reorganize (2026-05-17) — keep reports + marketing local
docs/reports/
docs/marketing/
docs/plans/css-organization-plan.md
docs/archive/

# Design assets từ Claude Design — keep local only
# (repo public — KHÔNG để PNG/HTML prototype leak ra ngoài)
docs/design/

# Local-only assets — không cần cho production Cloudflare deploy
# (2026-05-17) data prep tools + raw curriculum private
scripts/
content/

# Backend Edge Functions — deployed via Supabase Dashboard manual,
# không liên quan Cloudflare. (2026-05-17 user choice: keep local-only)
# CẢNH BÁO: file chỉ tồn tại local — mất laptop = mất source code.
# Phải copy text trong Supabase Dashboard về để recreate nếu cần.
supabase/

# Temp word lists
_hsk*_existing.txt

# Backup / temp files
*.backup
*.bak
*.orig
*~

# Database dumps
*.dump
*.sqlite
*.db

# Defensive: secret patterns (chưa có nhưng phòng tương lai)
.env
.env.*
*.pem
*.p12
*.pfx
*.key
credentials.json
service_account*.json
firebase-adminsdk-*.json
config.local.*
*.local.json

# Claude Code bootstrap (working style — auto-loaded by Claude Code)
CLAUDE.md
.claude/
